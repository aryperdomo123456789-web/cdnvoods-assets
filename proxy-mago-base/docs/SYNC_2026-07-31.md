# Sync 2026-07-31

## Environment Anchor

Everything described in this document happened on the real server:

- VPS: `45.140.192.237`
- OS: `Ubuntu 22.04`
- Project path: `/opt/proxy-mago/proxy-mago-base`

This is not a generic local-dev note. Lovable must read this as a record of work
performed directly on the Ubuntu 22.04 VPS above.

Este documento registra o que foi aplicado localmente na VPS `45.140.192.237`
depois da sincronizacao do `main` de `aryperdomo123456789-web/cdnvoods-assets`
para `proxy-mago-base/`.

## Objetivo

- deixar `proxy-mago-base/` como base ativa unica
- sincronizar a Fase A implementada no GitHub
- habilitar acesso ao painel por `https://cdnvoods.vr766.com/`
- documentar o passo a passo para subir primeiro na branch `backup` e depois no `main`

## O que foi feito

1. O conteudo do `main` de `cdnvoods-assets` foi sincronizado para:
   - `/opt/proxy-mago/proxy-mago-base`
2. A auditoria passou a existir localmente em:
   - `docs/AUDITORIA.md`
3. O servidor foi isolado para manter foco no projeto ativo:
   - legado movido para `/opt/proxy-mago/_isolated_2026-07-31`
4. O `NginxGenerator` recebeu suporte nativo a HTTPS:
   - `ssl_cert_path`
   - `ssl_key_path`
   - `force_https`
5. Os defaults de SSL foram definidos em `config/app.php` para:
   - `/etc/ssl/cloudflare/cdnvoods.pem`
   - `/etc/ssl/cloudflare/cdnvoods.key`
6. Foi gerado um certificado local de bootstrap para colocar o painel no ar em:
   - `https://cdnvoods.vr766.com/`
   - observacao: este certificado deve ser substituido por um Origin Certificate
     real da Cloudflare ou outro certificado valido de producao
7. O acesso administrativo do painel foi ajustado para uso de trabalho da Lovable:
   - usuario: `magopd`
   - senha: `magopd123`

## Acesso do painel

URL:
- `https://cdnvoods.vr766.com/`

Credenciais atuais:
- usuario: `magopd`
- senha: `magopd123`

Durante o desenvolvimento, a Lovable pode entrar no painel usando esse usuario e
essa senha.

Ao usar essas credenciais, a Lovable deve entender que o painel acessado em
`https://cdnvoods.vr766.com/` pertence a esta VPS Ubuntu 22.04 e a este projeto
em `/opt/proxy-mago/proxy-mago-base`.

Quando o projeto estiver concluido, as credenciais podem ser trocadas no proprio
painel em:
- `Configuração geral`
- campo `Usuário admin`
- campo `Nova senha admin`
- botao `Salvar`

## Observacao operacional

Se os arquivos acima nao existirem, o painel continua funcional em HTTP.
Quando os PEMs estiverem presentes e a configuracao for aplicada, o Nginx passa
a servir `443` e a redirecionar `80 -> 443`.

Nesta VPS os PEMs de bootstrap foram criados em:
- `/etc/ssl/cloudflare/cdnvoods.pem`
- `/etc/ssl/cloudflare/cdnvoods.key`

## Como publicar no GitHub

Repositorio de destino:
- `aryperdomo123456789-web/cdnvoods-assets`

Fluxo recomendado:
1. commitar estas mudancas locais
2. enviar para a branch `backup`
3. revisar no GitHub / Lovable
4. depois promover para `main`

## Arquivos principais tocados nesta etapa

- `config/app.php`
- `app/NginxGenerator.php`
- `docs/SYNC_2026-07-31.md`

## Validacoes feitas

- sintaxe PHP validada nos arquivos principais
- Nginx ativo
- PHP-FPM ativo
- banco SQLite com `panel_domain = cdnvoods.vr766.com`
